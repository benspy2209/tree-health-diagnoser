
import { useLanguage } from "@/i18n/LanguageContext";
import { Button } from "@/components/ui/button";

interface LanguageOption {
  value: "fr" | "en" | "nl";
  label: string;
}

export function LanguageSwitcher() {
  // Return null instead of the language buttons
  return null;
}
